{% comment %}
  sections/scent-reveal.liquid
  Scent profile reveal section for product pages.

  Metafield: custom.scent_profile_html  (type: single_line_text OR url)
  Accepted values:
    a) Full Shopify Files CDN URL — upload via Settings → Files, paste URL
    b) Just a theme asset filename: scent-profile-aura.html
  The section JS fetches the content and loads it via a Blob URL so the
  text/html MIME type is always correct (works even with Shopify's
  .html.liquid CDN rename).
{% endcomment %}

{%- comment -%} Resolve HTML URL — filename → asset_url, http(s) URL → as-is {%- endcomment -%}
{%- assign _scent_meta    = product.metafields.custom.scent_profile_html -%}
{%- assign scent_html_url = '' -%}
{%- if _scent_meta != blank -%}
  {%- assign _meta_val = _scent_meta.value -%}
  {%- if _meta_val.url != blank -%}
    {%- assign scent_html_url = _meta_val.url -%}
  {%- elsif _meta_val != blank -%}
    {%- assign _meta_str = _meta_val | downcase -%}
    {%- if _meta_str contains 'https://' or _meta_str contains 'http://' -%}
      {%- assign scent_html_url = _meta_val -%}
    {%- else -%}
      {%- assign scent_html_url = _meta_val | asset_url -%}
    {%- endif -%}
  {%- endif -%}
{%- endif -%}

{% if scent_html_url != blank %}

{%- liquid
  assign sec_id = section.id
  assign layout = section.settings.layout
-%}

<div class="sr-outer sr-outer--{{ layout }}" id="sr-{{ sec_id }}">
  <div class="sr-inner">

    {%- if section.settings.heading != blank or section.settings.subheading != blank -%}
    <div class="sr-heading-wrap">
      {%- if section.settings.heading != blank -%}
        <{{ section.settings.heading_tag }} class="sr-heading">
          {{ section.settings.heading }}
        </{{ section.settings.heading_tag }}>
      {%- endif -%}
      {%- if section.settings.subheading != blank -%}
        <p class="sr-subheading">{{ section.settings.subheading }}</p>
      {%- endif -%}
    </div>
    {%- endif -%}

    <div class="sr-iframe-wrap">
      <iframe
        id="sr-html-frame-{{ sec_id }}"
        class="sr-iframe"
        frameborder="0"
        scrolling="no"
        title="{{ product.title | escape }} — Scent Profile"
        allow="autoplay"
      ></iframe>
    </div>

  </div>
</div>

<style>
  /* ── Outer section ── */
  #sr-{{ sec_id }} {
    background-color: transparent;
    margin-top: {{ section.settings.margin_top }}px;
    margin-bottom: {{ section.settings.margin_bottom }}px;
    padding-left: {{ section.settings.margin_left }}px;
    padding-right: {{ section.settings.margin_right }}px;
  }
  #sr-{{ sec_id }} .sr-inner {
    background-color: {{ section.settings.bg_color }};
    padding-top: {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom }}px;
    border-radius: {{ section.settings.section_radius }}px;
    {%- if layout == 'full' -%}
      width: 100%;
      padding-left: {{ section.settings.padding_h }}px;
      padding-right: {{ section.settings.padding_h }}px;
    {%- elsif layout == 'boxed' -%}
      max-width: {{ section.settings.container_width }}px;
      margin-left: auto;
      margin-right: auto;
      padding-left: {{ section.settings.padding_h }}px;
      padding-right: {{ section.settings.padding_h }}px;
    {%- elsif layout == 'custom' -%}
      max-width: {{ section.settings.custom_width }}px;
      margin-left: auto;
      margin-right: auto;
      padding-left: {{ section.settings.padding_h }}px;
      padding-right: {{ section.settings.padding_h }}px;
    {%- endif -%}
  }

  /* ── Heading ── */
  #sr-{{ sec_id }} .sr-heading-wrap {
    text-align: {{ section.settings.heading_align }};
    margin-bottom: {{ section.settings.heading_gap }}px;
  }
  #sr-{{ sec_id }} .sr-heading {
    font-family: {{ section.settings.heading_font }};
    font-size: {{ section.settings.heading_size }}px;
    font-weight: {{ section.settings.heading_weight }};
    color: {{ section.settings.heading_color }};
    line-height: 1.15;
    margin: 0 0 {{ section.settings.subheading_gap }}px;
    letter-spacing: {{ section.settings.heading_tracking }}em;
  }
  #sr-{{ sec_id }} .sr-subheading {
    font-family: {{ section.settings.heading_font }};
    font-size: {{ section.settings.subheading_size }}px;
    color: {{ section.settings.subheading_color }};
    margin: 0;
    line-height: 1.5;
    letter-spacing: 0.02em;
  }

  /* ── Iframe ── */
  #sr-{{ sec_id }} .sr-iframe-wrap {
    width: {{ section.settings.iframe_width | default: 100 }}%;
    max-width: {{ section.settings.iframe_max_width | default: 520 }}px;
    margin: 0 auto;
  }
  #sr-{{ sec_id }} .sr-iframe {
    display: block;
    width: 100%;
    height: {{ section.settings.iframe_height | default: 460 }}px;
    border: none;
    background: transparent;
  }
  @media (max-width: 749px) {
    #sr-{{ sec_id }} .sr-iframe-wrap { width: 100%; }
    #sr-{{ sec_id }} .sr-iframe { height: {{ section.settings.iframe_height_mobile | default: 420 }}px; }
    #sr-{{ sec_id }} .sr-heading { font-size: {{ section.settings.heading_size | times: 0.75 | round }}px; }
  }
</style>

<script>
(function () {
  var iframe = document.getElementById('sr-html-frame-{{ sec_id }}');
  var src    = {{ scent_html_url | json }};
  if (!iframe || !src) return;

  fetch(src, { mode: 'cors', credentials: 'omit' })
    .then(function (r) { return r.ok ? r.text() : Promise.reject(r.status); })
    .then(function (html) {
      var blob    = new Blob([html], { type: 'text/html' });
      var blobUrl = URL.createObjectURL(blob);
      iframe.src  = blobUrl;
      iframe.addEventListener('load', function () { URL.revokeObjectURL(blobUrl); }, { once: true });
    })
    .catch(function () {
      iframe.src = src; /* direct fallback */
    });
})();
</script>

{% endif %}

{% schema %}
{
  "name": "Scent reveal",
  "tag": "section",
  "settings": [

    { "type": "header", "content": "Layout" },
    { "type": "select", "id": "layout", "label": "Container type",
      "options": [
        { "value": "full",   "label": "Full width" },
        { "value": "boxed",  "label": "Boxed (page container)" },
        { "value": "custom", "label": "Custom width" }
      ],
      "default": "boxed"
    },
    { "type": "range",  "id": "container_width", "label": "Boxed max width (px)",  "min": 600,  "max": 1600, "step": 20,  "default": 1200 },
    { "type": "range",  "id": "custom_width",    "label": "Custom max width (px)", "min": 300,  "max": 1600, "step": 20,  "default": 800 },
    { "type": "range",  "id": "padding_top",     "label": "Padding top (px)",      "min": 0,    "max": 160,  "step": 4,   "default": 60 },
    { "type": "range",  "id": "padding_bottom",  "label": "Padding bottom (px)",   "min": 0,    "max": 160,  "step": 4,   "default": 60 },
    { "type": "range",  "id": "padding_h",       "label": "Padding sides (px)",    "min": 0,    "max": 100,  "step": 4,   "default": 20 },
    { "type": "range",  "id": "margin_top",      "label": "Margin top (px)",       "min": 0,    "max": 160,  "step": 4,   "default": 0 },
    { "type": "range",  "id": "margin_bottom",   "label": "Margin bottom (px)",    "min": 0,    "max": 160,  "step": 4,   "default": 0 },
    { "type": "range",  "id": "margin_left",     "label": "Margin left (px)",      "min": 0,    "max": 200,  "step": 4,   "default": 0 },
    { "type": "range",  "id": "margin_right",    "label": "Margin right (px)",     "min": 0,    "max": 200,  "step": 4,   "default": 0 },
    { "type": "range",  "id": "section_radius",  "label": "Section radius (px)",   "min": 0,    "max": 40,   "step": 2,   "default": 0 },

    { "type": "header", "content": "Background" },
    { "type": "color",  "id": "bg_color", "label": "Background color", "default": "#f5f2ee" },

    { "type": "header", "content": "Heading" },
    { "type": "text",   "id": "heading",     "label": "Heading",    "placeholder": "e.g. Scent Profile" },
    { "type": "text",   "id": "subheading",  "label": "Subheading", "placeholder": "e.g. Tap to reveal notes" },
    { "type": "select", "id": "heading_tag", "label": "Heading tag",
      "options": [
        { "value": "h1", "label": "H1" },
        { "value": "h2", "label": "H2" },
        { "value": "h3", "label": "H3" },
        { "value": "p",  "label": "Paragraph" }
      ],
      "default": "h2"
    },
    { "type": "select", "id": "heading_align", "label": "Alignment",
      "options": [
        { "value": "left",   "label": "Left"   },
        { "value": "center", "label": "Center" },
        { "value": "right",  "label": "Right"  }
      ],
      "default": "left"
    },
    { "type": "select", "id": "heading_font", "label": "Heading font",
      "options": [
        { "value": "Manrope, sans-serif",          "label": "Manrope"        },
        { "value": "'Libre Baskerville', serif",   "label": "Baskerville"    },
        { "value": "Georgia, serif",               "label": "Georgia"        },
        { "value": "'Helvetica Neue', sans-serif", "label": "Helvetica Neue" }
      ],
      "default": "Manrope, sans-serif"
    },
    { "type": "range",  "id": "heading_size",     "label": "Heading size (px)",          "min": 14, "max": 80, "step": 2, "default": 28 },
    { "type": "select", "id": "heading_weight",   "label": "Heading weight",
      "options": [
        { "value": "300", "label": "Light 300"     },
        { "value": "400", "label": "Regular 400"   },
        { "value": "500", "label": "Medium 500"    },
        { "value": "600", "label": "Semi-bold 600" },
        { "value": "700", "label": "Bold 700"      }
      ],
      "default": "400"
    },
    { "type": "color",  "id": "heading_color",    "label": "Heading color",              "default": "#1a1a1a" },
    { "type": "range",  "id": "heading_tracking", "label": "Letter spacing ×0.01em",     "min": -5, "max": 20, "step": 1, "default": 0 },
    { "type": "range",  "id": "subheading_size",  "label": "Subheading size (px)",       "min": 10, "max": 28, "step": 1, "default": 14 },
    { "type": "color",  "id": "subheading_color", "label": "Subheading color",           "default": "#8b7355" },
    { "type": "range",  "id": "heading_gap",      "label": "Space below heading (px)",   "min": 0,  "max": 80, "step": 4, "default": 24 },
    { "type": "range",  "id": "subheading_gap",   "label": "Space below title (px)",     "min": 0,  "max": 40, "step": 2, "default": 8 },

    { "type": "header",    "content": "Scent profile iframe" },
    { "type": "paragraph", "content": "Set the product metafield 'custom.scent_profile_html' (single-line text) to a Shopify Files CDN URL or a theme asset filename (e.g. scent-profile-aura.html). The section fetches the file and loads it via a Blob URL so the MIME type is always correct." },
    { "type": "range", "id": "iframe_height",        "label": "Height — desktop (px)", "min": 300, "max": 800, "step": 10, "default": 460, "unit": "px" },
    { "type": "range", "id": "iframe_height_mobile", "label": "Height — mobile (px)",  "min": 280, "max": 700, "step": 10, "default": 420, "unit": "px" },
    { "type": "range", "id": "iframe_width",         "label": "Width %",               "min": 30,  "max": 100, "step": 5,  "default": 100 },
    { "type": "range", "id": "iframe_max_width",     "label": "Max width (px)",        "min": 300, "max": 900, "step": 20, "default": 520 }

  ],
  "presets": [{ "name": "Scent reveal" }]
}
{% endschema %}
